services:

  # One-shot: builds widget JS and deploys it to MinIO on every stack startup.
  # Exits after deploy (restart: no). Re-runs on `docker-compose up`.
  widget-deploy:
    image: ompharate/voxora-widget:latest
    restart: no
    env_file:
      - ../apps/widget/.env.docker
    environment:
      # Override endpoint to internal Docker hostname — no public port needed
      MINIO_ENDPOINT: minio
      MINIO_PORT: "9001"
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - voxora-network
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"

  web: 
    image: ompharate/voxora-web:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ../apps/web/.env.docker
    depends_on:
      api:
        condition: service_healthy
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  api:
    image: ompharate/voxora-api:latest
    restart: unless-stopped
    ports:
      - "3002:3002"
    env_file:
      - ../apps/api/.env.docker
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3002/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  ai:
    image: ompharate/voxora-ai:latest
    restart: unless-stopped
    env_file:
      - ../apps/ai/.env.docker
    depends_on:
      mongodb:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_started
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  mongodb:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"  # Only accessible from localhost for security
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE:-voxora-chat}
    volumes:
      - mongodb_data:/data/db
      - mongodb_backups:/backups
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '1.0'
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"  # Only accessible from localhost
    volumes:
      - redis_data:/data
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    command: >
      sh -c "redis-server 
      --appendonly yes 
      --requirepass $${REDIS_PASSWORD}
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru"
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test: ["CMD-SHELL", "redis-cli --no-auth-warning -a $$REDIS_PASSWORD ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    ports:
      - "9001:9001"  # API port (widget CDN)
      - "9002:9002"  # Console port
    environment:
      # MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      # MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      # MINIO_SERVER_URL makes MinIO sign presigned URLs with the public IP.
      # Without this, signatures are computed for host "minio" (internal) and
      # browsers get 403 because the Host header doesn't match the signature.
      MINIO_SERVER_URL: ${MINIO_PUBLIC_URL}
      MINIO_API_CORS_ALLOW_ORIGIN: "*"
    command: server /data --console-address ":9002" --address ":9001"
    volumes:
      - minio_data:/data
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9001/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:6333:6333"  # REST API
      - "127.0.0.1:6334:6334"  # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    networks:
      - voxora-network
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  mongodb_data:
    driver: local
  mongodb_backups:
    driver: local
  redis_data:
    driver: local
  minio_data:
    driver: local
  qdrant_data:
    driver: local

networks:
  voxora-network:
    driver: bridge

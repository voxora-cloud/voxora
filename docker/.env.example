# ============================================================
# docker/.env.example  —  Compose variable substitution template
# ============================================================
# Copy to docker/.env and fill in values before running:
#   cp .env.example .env
#   docker-compose up -d
# ============================================================

MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=

REDIS_PASSWORD=

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=

# Public URL browsers use to reach MinIO — REQUIRED for presigned URLs to work.
# Must match the public IP/domain:port where MinIO is reachable.
# e.g. http://3.111.24.80:9001
MINIO_PUBLIC_URL=http://YOUR_SERVER_IP:9001

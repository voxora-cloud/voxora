# ============================================================
# docker/.env  —  Docker Compose variable substitution
# ============================================================
# These vars are interpolated into docker-compose.yml for the
# infrastructure services (MongoDB, Redis, MinIO).
# This file is GITIGNORED — never commit real credentials.
# See .env.example for the template.
# ============================================================

# Shared MongoDB credentials (used by MongoDB container + MONGODB_URI in services)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=dev123

# Shared Redis password (used by Redis container + REDIS_PASSWORD in services)
REDIS_PASSWORD=dev123

# MinIO credentials
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Public URL browsers use to reach MinIO (presigned URLs are signed with this host)
# Dev: http://localhost:9001
# Production: http://YOUR_SERVER_IP:9001
MINIO_PUBLIC_URL=http://YOUR_SERVER_DOMAIN:9001
# ── App ────────────────────────────────────────────
NODE_ENV=production                         # production inside docker container
PORT=3002
CLIENT_URL=http://localhost:5173            # URL of the console (frontend)
PUBLIC_API_URL=http://localhost:3002        # Public-facing API URL
SERVICE_NAME=interaone-gateway              # Optional. Label used in structured logs.
LOG_LEVEL=debug                             # debug | info | warn | error.
INTERAONE_MODE=self-host                    # self-host | cloud

# ── Dodo Payments ──────────────────────────────────
DODO_PAYMENTS_API_KEY=
DODO_PAYMENTS_PRODUCT_PRO=
DODO_PAYMENTS_PRODUCT_PROPLUS=
DODO_PAYMENTS_WEBHOOK_SECRET=


# ── Database ───────────────────────────────────────
MONGODB_URI=mongodb://admin:dev123@mongodb:27017/interaone?authSource=admin


# ── Redis ──────────────────────────────────────────
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=dev123


# ── JWT ────────────────────────────────────────────
JWT_SECRET=interaone-dev-jwt-secret-key-2025
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=interaone-dev-refresh-secret-key-2025
JWT_REFRESH_EXPIRES_IN=30d


# ── Email ──────────────────────────────────────────
# Providers: mailhog (local SMTP dev server) | ses (AWS SES) | disabled
EMAIL_PROVIDER=mailhog
EMAIL_FROM_NAME=InteraOne Support
EMAIL_FROM_EMAIL=noreply@interaone.app


# ── AWS (SES email + SNS webhooks) ─────────────────
# Required when EMAIL_PROVIDER=ses or when using the AWS SES channel.
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=


# ── MinIO (Object Storage) ─────────────────────────
MINIO_ENDPOINT=minio
MINIO_PORT=9001
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=interaone

# Publicly accessible base URL for uploaded files.
MINIO_PUBLIC_URL=http://localhost:9001


# ── Rate Limiting ──────────────────────────────────
RATE_LIMIT_WINDOW_MS=60000                 # Window in ms
RATE_LIMIT_MAX_REQUESTS=1000               # Max requests per window


# ── Internal Security ──────────────────────────────
# Shared secret between the gateway and agent services for AI tool authentication.
AI_TOOL_SECRET=interaone_default_secret_2026

ALLOWED_DOMAINS=
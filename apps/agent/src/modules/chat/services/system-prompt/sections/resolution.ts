export const RESOLUTION_SECTION = `<resolution>
When query is resolved: call mark_query_resolved, give concise summary, continue normally. Never imply conversation is closed. If the visitor's message contained multiple sub-requests, only mark resolved once all parts have been addressed.
</resolution>`;
# Widget deploy script — Docker/Production environment
# Used by deploy-to-minio.js which runs on the HOST (not inside a container).
# MINIO_ENDPOINT = server IP (MinIO port 9001 must be accessible).
# API_URL_PRODUCTION = public API URL embedded into the widget bundle.
# Safe to commit: secret keys left blank (fill on server).

MINIO_ENDPOINT=YOUR_SERVER_IP
MINIO_PORT=9001
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=
API_URL_PRODUCTION=http://YOUR_SERVER_IP:3002

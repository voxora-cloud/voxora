
NODE_ENV=development
PORT=3002
API_URL=http://localhost:3002/api

# MongoDB Configuration
MONGODB_URI=mongodb://admin:dev123@localhost:27017/voxora-chat-dev?authSource=admin

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=dev123

# JWT Configuration (Development - Use strong secrets in production)
JWT_SECRET=voxora-dev-jwt-secret-key-2025
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=voxora-dev-refresh-secret-key-2025
JWT_REFRESH_EXPIRES_IN=30d

MAX_FILE_SIZE=10485760

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=60000 # minute
RATE_LIMIT_MAX_REQUESTS=1000 # Increased limit for development, adjust for production

# Email Configuration
# EMAIL_PROVIDER options: mailhog | smtp | resend | sendgrid | ses
# Use "mailhog" for local development (targets localhost:1025, no auth required)
EMAIL_PROVIDER=mailhog
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM_NAME=Voxora Support
EMAIL_FROM_EMAIL=noreply@voxora.com

# Provider credentials — set the vars for whichever EMAIL_PROVIDER you chose above.

# Use when EMAIL_PROVIDER=resend
RESEND_API_KEY=

# Use when EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=

# Use when EMAIL_PROVIDER=ses (Amazon SES)
# The IAM user must have ses:SendEmail permission on the verified sender identity.
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=
AWS_SES_REGION=us-east-1
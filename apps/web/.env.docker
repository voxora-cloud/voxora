# Web (Next.js) — Docker/Production environment
# Safe to commit: next.js NEXT_PUBLIC_* vars must point to the PUBLIC server IP
# because these are embedded into the browser bundle at build time.

NEXT_PUBLIC_API_URL=http://YOUR_SERVER_IP:3002/api/v1
NEXT_PUBLIC_SOCKET_URL=http://YOUR_SERVER_IP:3002
NEXT_PUBLIC_CDN_URL=http://YOUR_SERVER_IP:9001/voxora-widget/v1/voxora.js
NEXT_PUBLIC_ENV=production
